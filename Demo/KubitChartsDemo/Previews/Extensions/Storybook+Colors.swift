// swiftlint:disable:this file_name
import KubitCharts
import SwiftUI

extension Color {
    static var labelColor: Color {
        Color(hex: "#444444") ?? .gray
    }

    static var lineColor: Color {
        Color(hex: "#CCCCCC") ?? .gray
    }

    static var originLineColor: Color {
        Color(hex: "#666666") ?? .gray
    }
}
